# Generated from ontology object type 'fixed_asset'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: fixed_asset
    sql: SELECT * FROM read_parquet('s3://herosonecore-dev/raw/4db2b2a7cf464c9db355191393f3e23b/fixed_assets.parquet')

    joins:
      - name: department
        sql: "{CUBE}.department_id = {department}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: asset_no
        sql: asset_no
        type: string
      - name: name
        sql: name
        type: string
      - name: category
        sql: category
        type: string
      - name: department_id
        sql: department_id
        type: number
      - name: purchase_date
        sql: purchase_date
        type: time
      - name: original_value
        sql: original_value
        type: number
      - name: net_value
        sql: net_value
        type: number
      - name: status
        sql: status
        type: string
