# Generated from ontology object type 'project'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: project
    sql: SELECT * FROM read_parquet('s3://herosonecore-dev/raw/4db2b2a7cf464c9db355191393f3e23b/projects.parquet')

    joins:
      - name: department
        sql: "{CUBE}.department_id = {department}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: name
        sql: name
        type: string
      - name: department_id
        sql: department_id
        type: number
      - name: owner_id
        sql: owner_id
        type: number
      - name: budget
        sql: budget
        type: number
      - name: start_date
        sql: start_date
        type: time
      - name: end_date
        sql: end_date
        type: time
      - name: status
        sql: status
        type: string
      - name: progress_pct
        sql: progress_pct
        type: number
