# Generated from ontology object type 'lead'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: lead
    sql: SELECT * FROM read_parquet('/data/raw/leads.parquet')

    joins:
      - name: sales_rep
        sql: "{CUBE}.owner_rep_id = {sales_rep}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: name
        sql: name
        type: string
      - name: company
        sql: company
        type: string
      - name: source
        sql: source
        type: string
      - name: status
        sql: status
        type: string
      - name: owner_rep_id
        sql: owner_rep_id
        type: number
      - name: created_at
        sql: created_at
        type: time
