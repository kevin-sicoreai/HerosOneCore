# Generated from ontology object type 'bom'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: bom
    sql: SELECT * FROM read_parquet('/data/raw/boms.parquet')

    joins:
      - name: product
        sql: "{CUBE}.product_id = {product}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: product_id
        sql: product_id
        type: number
      - name: component_id
        sql: component_id
        type: number
      - name: quantity
        sql: quantity
        type: number
