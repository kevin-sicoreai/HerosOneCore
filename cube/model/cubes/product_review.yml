# Generated from ontology object type 'product_review'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: product_review
    sql: SELECT * FROM read_parquet('/data/raw/product_reviews.parquet')

    joins:
      - name: customer
        sql: "{CUBE}.customer_id = {customer}.id"
        relationship: many_to_one
      - name: product
        sql: "{CUBE}.product_id = {product}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: product_id
        sql: product_id
        type: number
      - name: customer_id
        sql: customer_id
        type: number
      - name: rating
        sql: rating
        type: number
      - name: tag
        sql: tag
        type: string
      - name: created_at
        sql: created_at
        type: time
