# Generated from ontology object type 'coupon'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: coupon
    sql: SELECT * FROM read_parquet('s3://herosonecore-dev/raw/4db2b2a7cf464c9db355191393f3e23b/coupons.parquet')

    joins:
      - name: customer
        sql: "{CUBE}.customer_id = {customer}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: code
        sql: code
        type: string
      - name: customer_id
        sql: customer_id
        type: number
      - name: amount
        sql: amount
        type: number
      - name: status
        sql: status
        type: string
      - name: issued_at
        sql: issued_at
        type: time
      - name: used_at
        sql: used_at
        type: time
