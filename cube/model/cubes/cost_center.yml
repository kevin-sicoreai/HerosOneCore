# Generated from ontology object type 'cost_center'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: cost_center
    sql: SELECT * FROM read_parquet('/data/raw/cost_centers.parquet')

    joins:
      - name: department
        sql: "{CUBE}.department_id = {department}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: code
        sql: code
        type: string
      - name: name
        sql: name
        type: string
      - name: department_id
        sql: department_id
        type: number
