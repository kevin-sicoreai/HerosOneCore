# Generated from ontology object type 'vehicle'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: vehicle
    sql: SELECT * FROM read_parquet('/data/raw/vehicles.parquet')

    joins:
      - name: department
        sql: "{CUBE}.department_id = {department}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: plate_no
        sql: plate_no
        type: string
      - name: type
        sql: type
        type: string
      - name: department_id
        sql: department_id
        type: number
      - name: status
        sql: status
        type: string
      - name: mileage
        sql: mileage
        type: number
      - name: purchased_at
        sql: purchased_at
        type: time
