# Generated from ontology object type 'delivery_route'. Do not edit by hand —
# regenerate with `python -m app.tools.generate_cube_schema`.
cubes:
  - name: delivery_route
    sql: SELECT * FROM read_parquet('/data/raw/delivery_routes.parquet')

    joins:
      - name: carrier
        sql: "{CUBE}.carrier_id = {carrier}.id"
        relationship: many_to_one

    dimensions:
      - name: id
        sql: id
        type: number
        primary_key: true
      - name: name
        sql: name
        type: string
      - name: from_city
        sql: from_city
        type: string
      - name: to_city
        sql: to_city
        type: string
      - name: carrier_id
        sql: carrier_id
        type: number
      - name: avg_days
        sql: avg_days
        type: number
      - name: cost_per_kg
        sql: cost_per_kg
        type: number
